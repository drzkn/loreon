echo "🔨 Running build and linting before push..."
yarn build

if [ $? -ne 0 ]; then
  echo "❌ Build failed. Push cancelled."
  exit 1
fi

echo "✅ Build and linting successful. Continuing with push..." 